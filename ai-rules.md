# AI開発行動規約 (ai-rules.md)

## 1. 編集範囲の制限

- 指示されたファイル、または実装に直接関係するファイル以外は絶対に勝手に書き換えないこと。
- 新しくファイルを作成・修正する場合は、必ず事前に確認を取ること。

## 2. 命名規則・設計の厳格な順守

- 一度決定したデータベースのカラム名、テーブル名、クラス名、関数（メソッド）名、エンドポイントのURL（`/api/...`）は、**以降のフェーズでも必ず完全に順守**すること。勝手に変更してはならない。
- 命名の選定に迷った場合や、より適切な命名の提案がある場合は、実装する前に必ずユーザーに相談すること。

## 3. コード提示時のルール（差分の明示）

- 既存のファイルを修正するコードを提示する際は、**必ずどの部分を変更するのか差分（追加・削除・修正の対象行）が明確に分かる形式**で提示すること。
- コードを提示する際は、どのファイルのどこに記述するのか（ファイルパスやクラス名）を明記すること。
- Javaのコードを書く際は、標準的な命名規則（クラス名は大文字始まりのCamelCase、メソッド・変数名は小文字始まりのcamelCase）に従うこと。

## 4. 不確実な場合の挙動

- 実装方法や要件に曖昧な点がある場合、勝手に推測してコードを書かず、必ずユーザーに質問すること。
- エラーが発生した際、原因が特定できない場合は複数の可能性を提示し、次のアクションを仰ぐこと。

## 5. 回答のスタイル

- IT初学者にも理解しやすいよう、専門用語は噛み砕いて説明すること。
